import ComingSoon from "@/components/ComingSoon";

export const metadata = { title: "Denník" };

export default function JournalPage() {
  return (
    <ComingSoon
      title="Denník"
      description="Reflexné zápisy: Situácia → Reakcia → Pocit → Čo to ukazuje → Lekcia → Princíp."
      stage="etape 2"
    />
  );
}
